{
  "name": "appcitas",
  "private": true,
  "description": "ÍntimaSalud - Orquestador raíz del entorno de desarrollo (ver plan/plan.md)",
  "scripts": {
    "dev": "docker compose --profile dev up --build --force-recreate",
    "dev:observability": "docker compose --profile dev --profile observability up --build",
    "down": "docker compose --profile dev --profile observability down --remove-orphans",
    "logs": "docker compose logs -f",
    "migrate": "docker compose exec backend alembic upgrade head",
    "makemigration": "docker compose exec backend alembic revision --autogenerate -m"
  }
}
